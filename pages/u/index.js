import React from 'react'

export default function DNE() {

    //     return <p>Loading...</p>;
    // } else {
    //     recipes = events.map(obj => {
    //         const { content, ...rest } = obj;
    //         return { ...rest, content: JSON.parse(content) };
    //     });

    // }
    return (
        <main >

            <article>

                go back. this doesn't exist
            </article>
        </main >
    )
}

